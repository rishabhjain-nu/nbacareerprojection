# v1 inference path.  No JAX, no Stan -- the Kalman recursions are numpy.
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=14
scikit-learn>=1.3
matplotlib>=3.8

# §5.3: the prior mean.  On macOS this needs libomp; if `import lightgbm` fails
# with a libomp.dylib error, `brew install libomp`.
lightgbm>=4.0

pytest>=7.4

# M6 only -- deliberately not required.  `pip install "numpyro[cpu]"` when a JAX
# wheel exists for your Python version.
# numpyro>=0.13
